export const TXN_CREATION_VALIDITY_MINUTES = 10;

export const DEFAULT_TXN_DISCOVERY_INTERVAL = "7 seconds";

export const DEFAULT_MESSAGE_OPTIONS = {
  disable_web_page_preview: true,
  parse_mode: "HTML",
} as const;

export const JOBS_TYPES = {
  EDITION_DISCOVERY: "edition-discovery",
  MINT_DISCOVERY: "mint-discovery",
};

export const DEFAULT_EDITION_SYMBOL = "$TELEGRAM_POST";
